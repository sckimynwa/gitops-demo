apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-yeoul.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-yeoul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-yeoul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
