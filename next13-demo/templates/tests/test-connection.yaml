apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next13-demo.fullname" . }}-test-connection"
  labels:
    {{- include "next13-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next13-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
